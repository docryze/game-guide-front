import SideMenu from './SideMenu.vue'
// 为组件提供install方法，用于全局注册
// SideMenu.install = (app) => {
//   app.component(SideMenu.name, SideMenu);
// };

// 默认导出组件
export default SideMenu

// 导出组件类型
// export * from './types';
